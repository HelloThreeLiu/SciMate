{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Spike 默认能力：仅核心 IPC（前端 invoke 与 fetch 内核）",
  "windows": ["main"],
  "permissions": ["core:default"]
}
